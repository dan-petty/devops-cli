# Task 589: Prompt-Injection Smuggling Detector and Canary Audit (`devops scan injection`)

**Issue**: [#589](https://github.com/dan-petty/devops-cli/issues/589)
**Status**: Backlog
**Milestone**: `v0.2.23`
**Priority**: `priority/p2-medium`
**Scope**: `type/feature`, `scope/cli`, `priority/p2-medium`

---

## 1. Description & Objectives

`normalize_unicode_text` (`src/devops_cli/ai/text_utils.py:36-41`, NFKC plus a 23-entry table dropping only U+200B/U+FEFF) runs on model output (`ai/review_schema.py:119-181`), not reviewed input. `sanitize_prompt_injection` (`security/sanitizer.py:296-302`) and `sanitize_prompt_boundary_tags` (`sanitizer.py:284-293`, wrapping review prompts at `ai/review/runner.py:1315`) match tag names only; `<|im_start|>`, `[INST]`, `<<SYS>>`, Tag-block, zero-width and bidi characters pass. Reviewed files are read `errors="replace"` (`runner.py:1292`) then `redact_text` (`runner.py:1494`), credential masking only; PR posting (`commands/review.py:645-661`) checks nothing outbound. `write_sarif` is called only at `commands/scan.py:839`; `scan sarif` (`scan.py:855-901`) ingests any SARIF 2.1.0 under `--suppress`/`--fail-on`, keying rule ids from bracketed titles (`security/normalization.py:29-32,171-185`). vibes `tools/prompt_injection_scanner.py` has INJ001 Tag-block decode (line 214), INJ002/INJ003/INJ005 and a `CanaryTokenManager` (`audit_response`, line 185, EGR005); vibes `observations/systems/10-mcp-protocol-boundaries-and-stateless-transport-constraints.md:53,125` names tool return strings as MCP's untaggable channel.

#### Key Deliverables:
- Context & Rationale*: `normalize_unicode_text` (`src/devops_cli/ai/text_utils.py:36-41`, NFKC plus a 23-entry table dropping only U+200B/U+FEFF) runs on model output (`ai/review_schema.py:119-181`), not reviewed input. `sanitize_prompt_injection` (`security/sanitizer.py:296-302`) and `sanitize_prompt_boundary_tags` (`sanitizer.py:284-293`, wrapping review prompts at `ai/review/runner.py:1315`) match tag names only; `<|im_start|>`, `[INST]`, `<<SYS>>`, Tag-block, zero-width and bidi characters pass. Reviewed files are read `errors="replace"` (`runner.py:1292`) then `redact_text` (`runner.py:1494`), credential masking only; PR posting (`commands/review.py:645-661`) checks nothing outbound. `write_sarif` is called only at `commands/scan.py:839`; `scan sarif` (`scan.py:855-901`) ingests any SARIF 2.1.0 under `--suppress`/`--fail-on`, keying rule ids from bracketed titles (`security/normalization.py:29-32,171-185`). vibes `tools/prompt_injection_scanner.py` has INJ001 Tag-block decode (line 214), INJ002/INJ003/INJ005 and a `CanaryTokenManager` (`audit_response`, line 185, EGR005); vibes `observations/systems/10-mcp-protocol-boundaries-and-stateless-transport-constraints.md:53,125` names tool return strings as MCP's untaggable channel.
- Deliverable*: PR A (additive): pure-function `security/prompt_injection.py` with INJ001 Tag-block decode, INJ002 format-character runs (`unicodedata.category(c) == "Cf"`), INJ003 delimiters (escaped, never deleted) and INJ005 exfiltration links; titles carry the bracketed rule id, so `normalize_finding`/`write_sarif` are untouched. Add `devops scan injection <target>` and a `scan_injection` MCP tool with their own `--sarif`/`--suppress`/`--fail-on`, calling `write_sarif` and `_load_policy_or_exit` (`scan.py:758`) directly since `_create_default_scanners` (`security/registry.py:16-44`) is subprocess-only (separate refactor). PR A also adds Ruff `PLE2502`, `PLE2510`, `PLE2512`–`PLE2515`, `RUF001`–`RUF003` to `pyproject.toml:76` (`PLE2511` does not exist). PR B (hot path): call the module from `_collect_file_blocks`/`_build_path_prompt` and diff pages, adding findings, not normalizing; a per-run `secrets.token_hex` canary in the review system prompt, `audit_response` over every finding's `title`/`description`/`fix`, the comment body before `pull.create_issue_comment` (`commands/review.py:661`) and auto-fix patches; a hit fails the run (EGR005). Port the rule taxonomy, `decode_unicode_tags`, delimiter list and canary shape; not INJ004 (flags every security test) or EGR001–EGR004 (duplicate `redact_text`/Gitleaks). Rejected (PyPI metadata): llm-guard 0.3.16 pulls torch/transformers/presidio-analyzer; rebuff last released 0.1.1 (2024-01-20). No maintained lightweight equivalent exists; stdlib `unicodedata` plus Ruff is the adopt path.
- Constraint*: Stripping is the wrong primitive: U+200C/U+200D are load-bearing in emoji, Indic and Arabic text. The review-side detector reports and continues; only a canary leak fails. Exempt U+E0000 (category Cn) and emoji tag sequences ending U+E007F by construct and offset, not extension. Its own fixtures will contain delimiters, so `scan injection` honours `security/suppression.py` via `--suppress`. The canary catches only a verbatim echo: a regression tripwire, not a proof. Audit for the token, not the guard block; never log it (`ai/mcp/dispatcher.py:20-31` CodeQL history). Sequence: PR A; then extend the Synthetic Prompt Adversaries corpus with Tag-block/zero-width variants; the v0.2.25 'MCP Resource-First Data Access & Tool Output Sandboxing' item imports this module for `_run_mcp_cmd` (`ai/mcp/server.py:45-77`) output.
- Measured*: `.venv/bin/python` walk over `src`, `tests`, `docs`, `scripts`, `k8s`, `.github`, `AGENTS.md`, `.pre-commit-config.yaml`, `pyproject.toml`: 1277 UTF-8 text files, none containing zero-width, Tag-block or bidi code points. Same probe: `normalize_unicode_text` passes 28 of 28 Tag-block characters (U+E0020–U+E003B) and both sanitizers leave `<|im_start|>`, `[INST]`, `<<SYS>>` unchanged. `grep -rn -i canary src/ tests/`: 13 lines, all Argo. `ruff --version` 0.16.8; `ruff rule` confirms the listed PLE/RUF ids exist and `PLE2511` does not.
- Source*: vibes `tools/prompt_injection_scanner.py`, `docs/ROADMAP.md` (lines 167–173), `observations/systems/10-mcp-protocol-boundaries-and-stateless-transport-constraints.md`
- Unit and integration test coverage with structural tuple equality assertions.
- Maintain cyclomatic complexity $M \le 10$ and nesting depth $\le 5$.
- 100% passing across Gated CI validation suite (`uv run devops ci`).
